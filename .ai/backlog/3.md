# Epic: Distribution & Hardening (Current)

## Summary

Distribution, hardening, and operational concerns — plus the deferred
Epic 2 items (per-session multi-LLM, ACP v2, persistence) once their
prerequisites stabilize or a need appears.

## Summary

Placeholder for the next epic after Multi-Provider & Advanced Protocol
Support: distribution, hardening, and operational concerns.

## Status

- **Priority:** P2 (now current — Epic 2 complete 2025-08-11)
- **Progress:** 0%

## Features

### Feature: Multiple LLMs per session (deferred from Epic 2)
- Per-session provider/model switching beyond the model KV — needs the
  stabilized v2 `providers/*` methods; the provider registry + session config
  are shaped for it
- **Tasks:**
  - [ ] `feature/multi-llm-per-session` — provider switching per session (deferred — wait for v2 providers/*)

### Feature: ACP v2 support (deferred from Epic 2)
- Re-evaluate against the then-current schema (2.0.0-alpha.2 as of 2025-08-11)
- **Tasks:**
  - [ ] `feature/acp-v2` — v2 schema types + method handlers (deferred — revisit when v2 stabilizes)

### Feature: Session persistence (if needed)
- Decide scope at planning (file snapshots vs DB); only if a real need appears
- **Tasks:**
  - [ ] `feature/session-persistence` — persistence design + implementation (backlog)

### Feature: Chat Completions adapter (candidate)
- OpenAI-compatible providers that predate the Responses API
- **Tasks:**
  - [ ] `feature/chat-completions-adapter` — Chat Completions implementation (backlog)

*(Other candidates: formal ACP conformance suite, performance/benchmarking,
config polish — e.g. provider health/monitoring, richer session history.)*
