# Epic: MVP ACP Server

## Summary

A usable-for-basic-use ACP v1 server: an agent host connects over stdio
(JSON-RPC 2.0), initializes with protocol version negotiation, runs
sessions/threads/turns with streaming text, backed by an OpenAI-compatible
Responses API (OpenAI or DeepSeek via `OPENAI_URL`). Env-only configuration —
no config file. Local dev deployment; cross-platform and CI releases deferred.

## Status

- **Priority:** P0
- **Progress:** 0%
- **Target:** none (no deadline)

## Features

### Feature: stdio JSON-RPC 2.0 transport (P0)
- **Use cases:** client sends requests/notifications on stdin; server responds on stdout; errors and `request_id` correlation
- **Requirements:** frame parse/serialize; request/response/notification/error objects; id-less notifications; batch handling if trivial
- **Dependencies:** none
- **Verification:** unit tests + round-trip fixtures (raw JSON → parsed → serialized)
- **Tasks:**
  - [x] `feature/stdio-jsonrpc-transport` — implement `protocol/json_rpc.zig` framing, `util/json.zig` helpers, stdin read loop in `main.zig` (done — merged 29893fd)

### Feature: initialize handshake + version negotiation (P0)
- **Use cases:** client negotiates protocol version and capabilities at startup
- **Requirements:** `initialize` handler per ACP v1 (`InitializeRequest` → `InitializeResponse`); `protocolVersion` (uint16) echoed/negotiated; capability surface honest to MVP; v2-ready dispatch seam
- **Dependencies:** F1
- **Verification:** captured client exchange transcript test; version mismatch behavior
- **Tasks:**
  - [ ] `feature/initialize-handshake` — `initialize` method handler + version routing in `protocol/methods.zig` (backlog)

### Feature: session/thread/turn lifecycle (P0)
- **Use cases:** client creates a session, opens a thread, runs turns with streaming progress
- **Requirements:** `session/new`, `thread/create`, `turn/create`; `turn/update` streaming notifications; `cancel` support; in-memory state store
- **Dependencies:** F1, F2
- **Verification:** scripted transcript tests (create → turn → stream → complete); cancel test
- **Tasks:**
  - [ ] `feature/session-thread-turn-lifecycle` — session/thread/turn types + handlers + in-memory store (backlog)

### Feature: OpenAI Responses provider adapter (P0)
- **Use cases:** turn text flows to the provider; deltas stream back as ACP updates
- **Requirements:** env config (`OPENAI_API_KEY`, `OPENAI_URL` default `https://api.openai.com/v1`, model — default `deepseek-chat` for DeepSeek usage); `util/http.zig` client; POST `/responses`; SSE parsing → `turn/update` content chunks; provider error mapping to ACP errors
- **Dependencies:** F3
- **Verification:** mock SSE server tests; live smoke test with user's DeepSeek key (manual)
- **Tasks:**
  - [ ] `feature/openai-responses-adapter` — `provider/adapter.zig` interface + `provider/openai.zig` Responses implementation (backlog)

### Feature: Tool-call round-trip (P1)
- **Use cases:** client provides tool definitions; model requests tool calls; client executes and returns results in a follow-up turn
- **Requirements:** ACP tool defs → OpenAI `tools` array; `function_call` output item → ACP `tool_call` content block; `tool_result` → `function_call_output` input item on next `/responses` call
- **Dependencies:** F4
- **Verification:** transcript fixture with tool call + result
- **Tasks:**
  - [ ] `feature/tool-call-roundtrip` — tool definition mapping + multi-call turn flow (backlog)

### Feature: GitHub Actions CI + releases (P2)
- **Use cases:** automated build/test; distributable binaries
- **Requirements:** matrix build (linux/mac/windows), `zig fmt --check` + `zig build test`, release artifacts on tag
- **Dependencies:** F1–F4
- **Verification:** green CI on push; artifacts on tag
- **Tasks:**
  - [ ] `feature/ci-releases` — `.github/workflows/ci.yml` + release workflow (backlog)

### Chore: conformance & cleanup
- **Use cases:** keep the quality bar (fmt/tests) as features land
- **Requirements:** `zig fmt` clean, `zig build test` green, pre-commit hook functioning
- **Dependencies:** all features
- **Verification:** hook passes on every commit
- **Tasks:**
  - [ ] `chore/epic1-cleanup` — final fmt/test pass, bench check for hot paths (stdio loop, SSE parse) (backlog)
